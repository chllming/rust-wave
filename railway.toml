[build]
builder = "RAILPACK"
buildCommand = "cargo build --locked --release -p wave-control-rust && install -m 755 target/release/wave-control-rust ./wave-control-rust"
watchPatterns = [
  "/services/wave-control-rust/**",
  "/crates/**",
  "/Cargo.toml",
  "/Cargo.lock",
]

[deploy]
startCommand = "./wave-control-rust"
healthcheckPath = "/healthz"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
