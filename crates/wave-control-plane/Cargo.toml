[package]
name = "wave-control-plane"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
# Compatibility forwarder over the reducer-backed projection contract.
wave-projections = { path = "../wave-projections" }
